# PLAN_REVIEW.md — adversarial review of EXECUTION_PLAN.md

Target: `docs/planning/EXECUTION_PLAN.md` (863 lines).
Reviewer: read-only session, 2026-09-12. No project file was modified.
Sources checked: `C:\repos\RamCore` (692 api sources + protocol + test modules), `C:\repos\RamRPG\src`,
`C:\repos\RamRPG\RAMRPG_ROADMAP_PROMPT.md`, `C:\repos\RamRPG\.github\workflows\ci.yml`,
`C:\repos\RamCore\ramcore-paper\build.gradle.kts`.

**Headline:** the plan's accuracy about *names and line numbers* is unusually good — I spot-checked ~40
RamCore symbols and ~20 RamRPG line citations and almost all are exact. What it gets wrong is
**runtime reality** (one BLOCKER that invalidates the entire content spine), **Gradle semantics** (the
spike was run under Maven and does not transfer), and **its own parallelism rule**, which three waves
violate outright.

---

## Findings

### BLOCKER

| id | plan line | finding | concrete fix |
|---|---|---|---|
| **B1** | 77-84, 107-108, 210-214, 218 | **Configurate is relocated in the shipped RamCore jar; every content WP will `NoClassDefFoundError` at runtime.** `C:\repos\RamCore\ramcore-paper\build.gradle.kts:73-74` does `relocate("org.spongepowered.configurate", "dev.willram.ramcore.libs.configurate")` and `relocate("com.typesafe.config", ...)`. But `dev.willram.ramcore.content.ContentDeserializer.deserialize(ConfigurationNode)` (`ContentDeserializer.java:22`), `ContentDefinition.node()` (`ContentDefinition.java:23`) and `RewardActionFactory.create(ConfigurationNode)` (`RewardActionFactory.java:29`) expose the **unrelocated** `org.spongepowered.configurate.ConfigurationNode` in `ramcore-api`. RamRPG compiled against ramcore-api emits `org/spongepowered/configurate/ConfigurationNode` in its bytecode; the installed RamCore plugin ships only `dev/willram/ramcore/libs/configurate/...`. RamCore's `paper-plugin.yml` declares no `libraries:` and there is no PluginLoader (`grep -rl PluginLoader ramcore-paper/src/main/java` → empty). Line 78-83 claims the spike "verified" the repoint — it verified **compilation only**; nothing in Phase F loads the jar against a real server before wave 5 dispatches WP-1.5a. This kills WP-1.5a, 1.5b, 1.2b, 2.1a, 3.1a, 3.4a, 4.1a, 5.1a, 6.1a, 6.3a and 7.1 — the entire "content is a file, not a release" thesis. | Insert **WP-F0** before F5: on a live Paper 26.1 server with RamCore 2.0.0 installed, call `ContentLoader.load(path)` and `RewardActionFactories.standard(economy)` from a throwaway RamRPG command and record the result. Expected outcome is `NoClassDefFoundError`. Then, per **rule 1**, patch upstream: drop the `configurate` / `typesafe.config` / `snakeyaml` relocate lines from `ramcore-paper/build.gradle.kts` (these are RamCore's own public API surface, unlike bStats which genuinely must be relocated), republish `2.0.1`, repoint F3. File it as decision **G8** and hard-gate wave 5 on it. Do **not** relocate configurate inside RamRPG to match — two plugin classloaders each holding `dev.willram.ramcore.libs.configurate.ConfigurationNode` yields `LinkageError`, not a fix. |
| **B2** | 100-105 (F2 sketch), 89 (F2 acceptance) | **`compileOnly` paper-api breaks test compilation; F2 cannot meet its own acceptance criterion.** Maven `provided` scope *is* on the test classpath; Gradle `compileOnly` is *not*. Three existing tests import paper-api types: `src/test/kotlin/dev/willram/ramrpg/ContentOverrideLoaderTest.kt:24-25` (`net.kyori.adventure.text.Component`, `org.bukkit.Material`), `StatRollTest.kt:12-13`, `StatServiceTest.kt:9`. F2's acceptance is "`./gradlew test` 26/26"; it will be 0/26 with unresolved references. The spike (line 78-83) was run under Maven and proves nothing about this. | Mirror RamCore's own module build, which already solves it: `C:\repos\RamCore\ramcore-api\build.gradle.kts:26` carries `testImplementation("io.papermc.paper:paper-api:$paperVersion")`. Add the identical line to the F2 sketch and name the three affected test files in F2's acceptance. |
| **B3** | 92 (WP-F5) | **`PlayerRpgData::copy` does not exist.** `core/storage/PlayerStore.kt:12` declares `class PlayerRpgData : DataItem()` — a plain class with `ConcurrentHashMap` fields, not a `data class`. There is no generated `copy`. `PlayerDataKey.of(id, type, defaultFactory, snapshot)` (`PlayerDataKey.java:62`) requires a `UnaryOperator<T>`. The WP as written does not compile. | Use the 3-arg overload `PlayerDataKey.of("rpg", PlayerRpgData::class.java, ::PlayerRpgData)` (`PlayerDataKey.java:47`) — its javadoc (`PlayerDataKey.java:17`) says mutable records keep the identity snapshot — **or** hand-write `fun snapshot(): PlayerRpgData` deep-copying the five mutable collections and pass `PlayerRpgData::snapshot`. Decide in F5's spec, not in the agent session. |
| **B4** | 92 (F5), 177-180 (1.2a), 430-432 (4.1b), 465 (5.1a) | **`Stores.jsonByUuid` has no `StoreMigrations` overload, so F5's chosen construction locks out every later `PlayerRpgData` migration.** `Stores.java:97` is `jsonByUuid(Path, Class<V>)` → `file(directory, DataKeyCodec.uuidKeys(), StoreCodec.gson(type))`, no migrations argument; the migration-capable overload is `Stores.file(Path, DataKeyCodec, StoreCodec, StoreMigrations)` (`Stores.java:84`). Three later WPs each need a `PlayerRpgData` migration: 1.2a (`QuestProgressMigrationTest`), 4.1b (explicitly "needs a `dataVersion` bump plus a `StoreMigrations` entry"), 5.1a (see M8). Each would have to re-do F5's store construction. | F5 builds the store as `Stores.cached(Stores.file(dir, DataKeyCodec.uuidKeys(), StoreCodec.gson(PlayerRpgData::class.java), migrations))` with an F5-owned `StoreMigrations<PlayerRpgData>` chain from `StoreMigrations.start()` (`StoreMigrations.java:48`). DESIGN.md records the ledger: v1 = today, v2 = quests (1.2a), v3 = buffs (4.1b), v4 = perks (5.1a); one owner per bump. |
| **B5** | 677-678 (waves 6-7) vs 702-703 | **Waves 6 and 7 violate the plan's own rule verbatim.** Rule (line 702): "never run two WPs concurrently if both touch `RamRPG.kt`, `api/items/Items.kt`, or `ItemSchema`." Wave 6 = {1.5b, 1.5c, 1.2b}: **all three** list `RamRPG.kt` as modified (lines 219, 227, 186), and 1.5b + 1.5c **both** modify `core/config/ContentLoader.kt` (219, 227). Wave 7 = {1.5d, 1.1b, 2.1a, 2.3a}: 1.5d, 1.1b and 2.3a all modify `RamRPG.kt` (234, 165, 298); 1.5d and 2.1a **both** modify `builtin/items/BuiltinItems.kt` **and** `src/main/resources/content/items/builtin.conf` (234, 267); and 2.1a modifies `api/items/Items.kt` (267) — the second forbidden file — alongside three peers. | Re-cut: wave 6a = 1.5b solo (it owns `ContentLoader.kt` + `Effects.kt` + `RamRPG.kt`); wave 6b = {1.5c, 1.2b}. 2.1a must precede 1.5d (1.5d's `builtin.conf` has to carry `itemLevel`/`requirements` already), so wave 7a = 2.1a solo, wave 7b = {1.5d, 1.1b, 2.3a}. Additionally, make WP-1.7 leave a per-feature registration seam (e.g. one `ServiceModule` file per subsystem bound via `RamPlugin.bindModule`, `RamPlugin.java:105`) so later WPs stop editing `RamRPG.kt` at all — Risk 4 (line 843) already promises this but no WP delivers it. |
| **B6** | 680 (wave 9) vs 702-703 | **Wave 9 = {2.1c, 2.2, 2.3b}: 2.1c and 2.2 both modify `api/items/Items.kt` and `core/services/StatProviders.kt`.** 2.1c adds `LoreSection.Requirements`/`.ItemLevel` + quality in the rarity line and edits `EquipmentStatProvider` (line 283); 2.2 adds `LoreSection.Durability` and edits `StatProviders.kt` for inert items (line 290). Both rewrite the same `LoreTemplate.DEFAULT` list at `api/items/Items.kt:189-199` and the same provider. The plan marks them mutually parallel-safe (283 "PS: 2.2", 287 "PS: 2.1c"). | Merge 2.1c and 2.2 into one WP — they share the inert-item concept (2.2's inert-at-zero-durability *is* 2.1c's inert-on-unmet-requirement, and 2.1c's own risk note demands a single inert path across every provider) — or serialize wave 9a = 2.1c, wave 9b = {2.2, 2.3b}. |

### MAJOR

| id | plan line | finding | concrete fix |
|---|---|---|---|
| **M1** | 672 (wave 1), 198, 246 | **Wave 1: 1.3 and 1.6b both modify `core/rendering/PacketItemRenderer.kt`** (1.3's file list at line 201, 1.6b's at 249), and the plan explicitly declares them parallel-safe with each other (198 "PS: … 1.6b", 246 "PS: … 1.3"). 1.6b is an `L` rewrite of that exact file. | Move 1.3's renderer edit out: 1.3 exposes `AbilityService.remaining(player, key): Duration` only; the lore/renderer consumption of it becomes part of 1.6b's spec, or a follow-on `1.3b` after 1.6b merges. Otherwise put 1.3 in wave 2. |
| **M2** | 683 (wave 12), 367, 380, 386 | **Wave 12: 3.3a, 3.3c and 3.3d all modify `core/listeners/SkillsCommand.kt`** (retiring `/skills upgrade`, `/skills reforge`, and socket subcommands respectively) in three concurrent worktrees. The file is 472 lines of Brigadier tree; three simultaneous subcommand deletions guarantee a structural merge conflict, which F.4 (line 819) says must be re-dispatched rather than hand-merged. | Add a tiny **WP-3.0** in wave 11 that strips all three subcommands from `SkillsCommand.kt` at once and leaves them as `@Deprecated` no-op stubs pointing at `/rpg`, so 3.3a/3.3c/3.3d only add recipes and never touch the command file. |
| **M3** | 693 (wave 22), 490, 502, 487 | **Wave 22: 6.1a and 6.2 both modify `core/listeners/EntitySpawnListener.kt`** and are declared mutually parallel-safe (487 "PS: 6.2", 502 "PS: 6.1a"). Both hook the same spawn path — 6.1a to roll affixes by tier, 6.2 to resolve the level band and scale the profile — and the band must resolve *before* the affix roll (tier weights come from the band, line 506). This is a real ordering dependency, not just a conflict. | Serialize: wave 22a = 6.2 (band resolution + caching on the entity), wave 22b = {6.1a, 5.1b, 5.1c}, with 6.1a declaring `Dep: 6.2`. Update the mermaid at 645-646. |
| **M4** | 90 (F3), 78-79, 248, 251 | **`ramcore-protocol` is `compileOnly`, but WP-1.6b's tests need it.** `InMemoryPacketVisualTransport`, `ProtocolVisualPacketFactory`, `PacketVisualTransport` and `Packets` all live in **ramcore-protocol** (`ramcore-protocol/src/main/java/dev/willram/ramcore/packet/*.java`), not ramcore-api. `ramcore-test/build.gradle.kts` only declares `api(project(":ramcore-api"))`, so nothing puts protocol on the test classpath. Line 78-79's claim that protocol is "needed only for `dev.willram.ramcore.protocol.Protocol`" is factually wrong. | F3 adds `testImplementation("dev.willram:ramcore-protocol:2.0.0")` alongside the `compileOnly`, and correct the line 78-79 note to "needed for `protocol.Protocol` **and** the whole `dev.willram.ramcore.packet` package (WP-1.6b)". |
| **M5** | 191 (WP-1.2c) vs roadmap line 89 | **Coverage gap: `StatsGui` and `SkillsGui` are never migrated.** Roadmap Task 1.6 bullet 1 names all three GUIs; the plan covers only `QuestsGui` (WP-1.2c). `core/listeners/StatsGui.kt:4-5` and `core/listeners/SkillsGui.kt:4-5` still import the legacy `dev.willram.ramcore.menu.Gui` / `menu.Item`, exactly like `QuestsGui.kt:4-5`. DoD item 1 ("no RPG-side duplicates of … menu plumbing") is therefore unmet at v2.1.0. | Add **WP-1.6c — StatsGui/SkillsGui on `MenuView`/`MenuSession`** (Sonnet, M, Dep 0.1, same wave as 1.2c), tests `StatsGuiLayoutTest` / `SkillsGuiPaginationTest` (pure `MenuView` slot math — see M10). |
| **M6** | 391-397 (WP-3.4a) | **WP-3.4a mostly re-adds fields that already exist, and its renames break `api/` (rule 7).** `api/enchants/Enchants.kt:19-30` already declares `maxLevel` (:22), `targets: Set<ItemCategory>` (:23), `rarity: EnchantmentRarity` (:24) and `fun conflicts(other: EnchantmentKey): Boolean` (:27). Only `weight: Int` and `skillRequirement: Int` are genuinely new. Renaming `targets` → `applicableCategories` and replacing `conflicts()` with `conflictsWith: Set<EnchantmentKey>` are breaking changes to a public interface with no deprecation cycle. As scoped, this is a ~20-line change occupying an **Opus solo wave** (wave 14). | Keep `targets` and `conflicts()`; add `val weight: Int get() = 10` and `val skillRequirement: Int get() = 0` as defaulted interface properties. Delete solo wave 14 and fold the change into 3.4b's first commit; 3.4d then depends on 3.4b instead of 3.4a. Retitle the WP "enchant pool metadata" so nobody re-derives the renames. |
| **M7** | 143-147 (1.1a), 178-179 (1.2a) | **Rule-7 breaches with no deprecation cycle.** WP-1.1a deletes `api.entities.LootEntry` and removes `EntityProfile.loot` / `.lootPool` / `.lootRolls` (`api/entities/Entities.kt:16-35`). WP-1.2a restructures `QuestDefinition` and its `QuestGoal` / `QuestReward` hierarchies (`api/quests/Quests.kt:14-35`). Roadmap rule 7 forbids breaking `api/` without a deprecation cycle; F.2 reviewer item 7 (line 787) makes this an automatic finding, so both WPs will bounce on review as specified. Only 1.7 (line 258) and 1.5a (line 211) budget a shim. | Either (a) state in section A that Phase 1 is an explicit one-off rule-7 waiver because `api/` has no external consumers (RamRPG is a leaf plugin — verify and write it down), or (b) spec the shims: keep `LootEntry` as `@Deprecated(…, ReplaceWith)` with an `EntityProfile.lootTable` fallback for one release, and keep `QuestDefinition(key, displayName, description, goal, rewards, daily, category)` as a `@Deprecated` secondary constructor that builds the `ObjectiveDefinition`. Pick one before wave 2 dispatches. |
| **M8** | 465 (WP-5.1a) vs 432 (WP-4.1b) | **WP-5.1a adds instance/player fields with no migration.** It adds `PlayerRpgData.perkRanks` and `.perkPointsSpent` (line 465) but its Tests/DESIGN line (467) names no migration test and no `dataVersion` bump — while 4.1b, adding one field to the same class, correctly demands "a `dataVersion` bump plus a `StoreMigrations` entry" (432) and ships `BuffPersistenceRoundTripTest`. | Add `PerkPersistenceMigrationTest` to 5.1a and an explicit "bumps `PlayerRpgData` dataVersion to v4; absent `perkRanks` defaults to empty, never null" line, matching 4.1b's wording. Fold into the DESIGN.md dataVersion ledger from B4. |
| **M9** | 188 (WP-1.2b) | **The test contradicts the RamCore contract.** `RewardActionFactory.create(ConfigurationNode)` is declared `throws ContentDeserializeException` (`RewardActionFactory.java:29`); the WP requires "bad params yield a `ValidationError`, never an exception". `ContentDeserializeException` is how RamCore's own `ContentLoader` collects per-file errors into `ValidationError`s. | Restate as: "`RpgRewardFactoryParseTest` asserts bad params throw `ContentDeserializeException` with a message naming the offending key, and that the surrounding `SpecLoader`/`ContentLoader` pass turns it into a `ValidationError` carrying `SourceRef` file+path". |
| **M10** | 335 (3.1b), 151 (1.1a), 277 (2.1b), 445 (4.2a), 492 (6.1a), 229 (1.5c), 474 (5.1b) | **Several proposed tests cannot be written off-server with the available fakes.** `ramcore-test` ships only `FakeScheduler`, `FakeClock`, `ProxyFakes`, `TestServiceContext`, `FakeItemStack`; `ProxyFakes` proxies **interfaces only** (`ProxyFakes.java:18`, `java.lang.reflect.Proxy`) and `FakeItemStack` "carries no material or meta" (`FakeItemStack.java:6-7`). Consequences: `MenuSession` calls `Bukkit.createInventory` (`MenuSession.java:78`), so `StationSlotStateTest` / `CraftPreviewTest` / `CraftConsumesInputsTest` cannot construct a session; `ItemInstanceService.create/read` needs `Bukkit.getItemFactory()` for PDC, so `RpgItemLootFunctionTest` (seeded determinism) and `QualityScalesRollsTest` cannot use real stacks; `Selectors` needs a `World`, so `SplashAreaSelectionTest` cannot run; entity PDC needs a live entity, so `AffixMetadataRoundTripTest` cannot run; `ValidateCommandOutputTest` needs a Brigadier harness the plan itself lists as a missing RamCore piece (line 849). | Re-scope each to its pure layer and say so in the WP: menu tests assert `MenuView`/`MenuState` slot and page math (no session); seed/quality tests assert `ItemInstanceServiceImpl.ItemDto` round-trips (the layer `ItemDtoTest`/`PdcSchemaStabilityTest` already use); splash tests assert the radius/filter predicate over fake `Position`s; affix tests assert the codec `String` ↔ `List<AffixDefinition>`; validate tests assert the returned `List<ValidationError>` rather than command output. Where that is not possible, mark the WP **BLOCKED on a RamCore gap** (`MenuClickContexts.fake`, `CommandTestHarness`) per F.4 row 3 and rule 1 — do not let an agent invent a mock. |
| **M11** | 420, 462, 487, 502 | **Four chain-of-convenience dependencies inflate the critical path by ~4 waves.** 4.1a `Dep: 3.4a`, 5.1a `Dep: 4.1a`, 6.1a `Dep: 5.1a`, 6.2 `Dep: 5.1a`. None is a real code dependency: the Buff API reads nothing from enchant pool metadata; region level bands (`LevelBandService`, `EntityProfileRegistryImpl.resolve`) read nothing from perks. They appear to exist only to keep the Opus solo waves single-file. | Re-parent: 4.1a `Dep: 1.5b` (it needs the Effect schema, nothing more); 5.1a `Dep: 4.1a` is real only for `RpgRewardActions` un-stubbing — keep it; 6.1a and 6.2 `Dep: 1.5b, 2.3a` (affix effects and band stat multipliers need the effect schema and damage types, not perks). That lets 6.2 run in wave 10 alongside 5.3 and collapses waves 21-23. |
| **M12** | 89 (F2), 828 (G2), 227-229 (1.5c), missing | **Three cross-cutting WPs are missing entirely: CI, permissions, and content-authoring docs.** (a) `.github/workflows/ci.yml` exists and runs `mvn -B -ntp … verify`; F2 says "update `.github/workflows/*`" but neither F2's acceptance nor any later checklist requires **CI green**, and `ramcore-api|-protocol|-test:2.0.0` resolve only from `mavenLocal()` — so CI is red from wave 0b until someone publishes. G2 recommends a JitPack tag "before Wave 1" but assigns it to no WP. (b) `src/main/resources/paper-plugin.yml` has **no `permissions:` block** (compare RamCore's, which declares `ramcore.diagnostics`), so `/rpg reload` and `/rpg validate` from WP-1.5c ship unguarded to every player. (c) RamRPG has **no `README.md`** and no content-schema reference; DoD item 2 ("a new item … can be added with a `.conf` file") has no artifact a non-Kotlin designer can read — `docs/DESIGN.md` is a numbers document by its own charter (line 124-133). | Add **WP-F7 — CI on Gradle + published RamCore** (Haiku, S, after F3): rewrite `ci.yml` to `./gradlew test` with the Gradle cache action, and resolve RamCore from the JitPack tag G2 calls for; F2/F3 acceptance gains "CI green on a push branch". Add **WP-1.5e — permission nodes** (Haiku, XS, with 1.5c): `ramrpg.command.reload`, `.validate`, `.admin` declared in `paper-plugin.yml` and asserted in `RpgCommand.kt`. Add **WP-7.5 — README.md + docs/CONTENT.md** (Sonnet, M, after 6.3c) documenting every `content/**` type with a worked example, as the DoD-item-2 proof. |
| **M13** | 254-260 (WP-1.7), 114, 843 | **WP-1.7 is XL, labelled L, and the plan's legend says XL "must be split before dispatch".** It rewrites **23** `lateinit` fields (`RamRPG.kt:89-112` — the plan says "~17" at lines 63 and 255), every `core/services/*.kt` constructor, all ~20 listeners, and four `RamRPG.get()` reach-throughs (`SkillsCommand.kt:255,275,283,413`). That is comfortably >600 LOC changed. Worse, Risk 4 (843) claims "after it, registration is per-service" — yet 15 later WPs still list `RamRPG.kt` as modified, which is precisely what causes B5. | Split into 1.7a (define `ServiceKey`s + register existing instances in `load()`, keep `RamRPG.get()` working — mechanical, Sonnet) and 1.7b (constructor-inject the services, delete the `lateinit` fields, `@Deprecated` the singleton — Opus). Make 1.7b's acceptance include "no later WP needs to edit `RamRPG.kt`": every subsystem gets its own `TerminableModule` bound via `RamPlugin.bindModule` (`RamPlugin.java:105`), and 1.7b adds an empty module file per planned subsystem so later WPs edit only their own. |

### MINOR

| id | plan line | finding | concrete fix |
|---|---|---|---|
| **m1** | 221 vs 842, 220 | **Trigger count is inconsistent and the schema omits one variant.** `api/effects/Effects.kt:44-53` declares 8 `EffectTrigger` variants today (`OnEquip`, `OnHit`, `OnHurt`, `OnKill`, `OnInteract`, `OnBlockBreak`, `Tick`, `Custom`); adding `OnConsume` makes 9. Line 221 says `TriggeredEffectDispatcherTest` covers "all eight triggers"; line 842 says "all eight triggers plus `OnConsume`". The HOCON trigger list at line 220 enumerates 8 forms and silently drops `Custom(ContentId)`. | Say "all nine triggers" in both places and add `custom:<content-id>` to the HOCON form list. |
| **m2** | 219 vs 444 | **`EffectTrigger.OnConsume` is added twice.** Both WP-1.5b (line 219, "modify `api/effects/Effects.kt` (add `EffectTrigger.OnConsume`)") and WP-4.2a (line 444, "modify `api/effects/Effects.kt` (`EffectTrigger.OnConsume`)") claim to introduce it. | Delete the clause from 4.2a; it consumes the trigger 1.5b already shipped. |
| **m3** | 220-222 (WP-1.5b) | **The effect schema has a registry for actions but not for conditions or block matchers.** `TriggeredEffect.conditions: List<Condition>` (`Effects.kt:85`) and `EffectTrigger.OnBlockBreak(matcher: BlockMatcher)` (`Effects.kt:50`) are both `fun interface`s with no serialisable form, yet the HOCON at line 220 contains `conditions=[...]` and `on_block_break:<matcher>`. First content WP that needs a condition (3.4d, 6.1b) hits the gap and, per the WP's own risk note, must escalate. | 1.5b registers three registries, not one: `EffectActionRegistry`, `EffectConditionRegistry`, and a `BlockMatcherRegistry` keyed by id (`BlockMatchers.ofMaterials` already exists at `Effects.kt:36`). Add `EffectConditionRegistryUnknownIdTest`. |
| **m4** | 63, 255 | "~17 `lateinit` services" — actual count is **23** (`grep -c lateinit src/main/kotlin/dev/willram/ramrpg/RamRPG.kt`). The roadmap says "~20". Understates WP-1.7's blast radius (see M13). | State 23 and list which are private. |
| **m5** | 88 (WP-F1) | **Adding `.claude/settings.local.json` to `.gitignore` does not untrack it** — the git status snapshot shows it as already-tracked-and-modified. The file will keep appearing in every later diff, defeating F1's purpose ("so every later WP has a clean diff"). | F1's steps: `git rm --cached .claude/settings.local.json` **and** add the `.gitignore` entries, in the same commit. |
| **m6** | 857-862 (section I) vs 665-700 (section E) | **Milestone tags do not land on wave boundaries.** v2.1.0 is "After Phase F + 0 + 1", but wave 7 mixes Phase-1 WPs (1.5d, 1.1b) with Phase-2 WPs (2.1a, 2.3a). v2.4.0 is "Phase 4 + 5.1", but wave 22 mixes 5.1b/5.1c with Phase-6 WPs (6.1a, 6.2). Neither tag can be cut without splitting a wave mid-flight. | Either re-cut waves 7 and 22 so each ends a phase, or restate the tags by wave number ("v2.1.0 = after wave 7a") in section I. |
| **m7** | 454 vs 692; 303 vs 680-681; 159 vs 677-678 | **PS annotations contradict the wave table.** 4.2c declares "PS: 5.1a" but 5.1a is a **solo** wave one wave later (line 692, and 462 says "PS: none"). 2.3b declares "PS: 5.3" but 2.3b is wave 9 and 5.3 is wave 10. 1.1b declares "PS: 1.2b" but 1.2b is wave 6 and 1.1b wave 7. | Regenerate the PS column mechanically from the wave table once the waves are re-cut (B5, B6, M1-M3), or delete the PS column and let the wave table be the single source. |
| **m8** | 732, 747 (F.1 template) vs roadmap line 22 | **Roadmap working rule 8 is silently replaced.** The roadmap's rule 8 is "Work one task at a time in the order given unless told otherwise. After each task, summarize what changed, what was tested, and open questions." The template's rule 8 is "Stay inside this work package", presented under the heading "NON-NEGOTIABLE RULES (from the roadmap)". The summarize half is honoured by REPORT BACK (755-765); the serialize half is deliberately abandoned for worktree parallelism — which is defensible but must be owned, not renumbered away. | Keep the roadmap's rule 8 text in the template and add a rule 9 ("Stay inside this work package"). State the parallelism deviation explicitly in section A: "the roadmap's one-task-at-a-time rule is replaced by the wave schedule in section E; rule 8's reporting half is preserved in the REPORT BACK block." |
| **m9** | 796-808 (F.3), 93 (F6), 18-26 (section A) | **The Definition of Done is never gated.** F.3 checks tests, review verdict, jar build and two greps, but nothing verifies DoD item 1 ("no RPG-side duplicates of loot, rewards, objectives, cooldowns, or menu plumbing"), and item 3 (the eleven-step player journey) has no owner. `docs/SMOKE_TEST.md` is authored at F6 against the 2.0 feature set and no later WP updates it, so the Folia/Paper column set never grows with the features it is supposed to prove. | Add F.3 item 11: "at each milestone tag, re-run the DoD checklist — `grep` for surviving RPG-side duplicates by package (`api/quests` progress tracking, `AbilityServiceImpl` cooldown map, `api/entities.LootEntry`), and walk DoD item 3's journey on a live server." Make every WP whose feature appears in the journey append its own scenario row to `docs/SMOKE_TEST.md` (add it to the F.1 template alongside the DESIGN.MD and LANG KEYS lines). |
| **m10** | 804 (F.3 item 7), 65 | **No WP backfills the existing hardcoded strings, and the grep gate is weak.** `lang/en_us.json` has 14 keys, all `ramrpg.stat.*`. Player-visible strings such as `Component.text("Ability disabled")`, `"On cooldown"`, `"Requirements not met"` (`core/services/AbilityServiceImpl.kt:70,75,81`) stay hardcoded forever, because rule 6 only demands a key for *new* strings. F.3 item 7 greps that `Component.text("` "did not grow" — defeated by `Component.text(variable)` and by any string moved into a helper, and it can never shrink. | Add **WP-0.2 — lang backfill** (Haiku, M, wave 1, Dep F5): every existing player-visible `Component.text` literal becomes `Component.translatable("ramrpg.<area>.<name>")` with an `en_us.json` entry, plus `LangKeyCoverageTest` asserting that every key referenced in `src/main/kotlin` exists in `en_us.json` and vice versa. Replace F.3 item 7 with running that test. |
| **m11** | 227-228 (1.5c), 367 / 380 / 386 | **`/rpg` vs `/skills` namespace has no owner.** `SkillsCommand.kt:255` already exposes a reload path (`RamRPG.get().reloadContent()`); 1.5c adds `/rpg reload`; 3.3a/3.3c/3.3d each retire one `/skills` subcommand. No WP defines the final command surface, moves `/skills stats|skills|quests` under `/rpg`, or gives `/skills` a deprecation cycle. | Fold into the WP-3.0 proposed in M2: define the `/rpg` tree once (`reload`, `validate`, `stats`, `skills`, `quests`, `perks`, `admin`), keep `/skills` as an aliasing shim for one release, and record the mapping in DESIGN.md. |
| **m12** | 242 (WP-1.6a) | `DisplaySpawner.spawn` returns `Promise<DisplayHandle<T>>` (`DisplaySpawner.java:18`) — `DisplayHandle` is generic. The WP's `RpgPresentation.damageIndicator(location, Component): DisplayHandle` drops the type parameter, which will not compile cleanly in Kotlin without a raw-type warning. | Spec it as `Promise<DisplayHandle<TextDisplay>>` (or a bound RPG wrapper), and keep the "bind every handle" risk note. |
| **m13** | 426 (4.1a), 572 (7.3) | **Two Folia hazards the plan does not name.** (a) 4.1a's expiry design ("one sweep on a fixed interval through `PlatformScheduler`") touches `LivingEntity`s spread across regions; on Folia a single sweep task cannot touch any of them — it must enumerate expired ids and re-dispatch one unit of work per entity context. (b) 7.3's `RealTimeScheduler` jobs fire on the scheduler's own thread; any job that messages players or resets objective progress must hop to each player's `TaskContext`. Neither risk row mentions it; F.2 reviewer item 3 would catch it only post-hoc. | Add the constraint to both WP risk lines and to the F.1 template's rule 4 ("a periodic sweep may compute on any thread but must re-dispatch one task per entity/player context before touching a Bukkit object"). |
| **m14** | 91 (F4) vs 857 (section I) | F4 sets `version = "2.1.0"` at wave 0d, but section I ties the v2.1.0 tag to the completion of Phase 1 (wave 7). The jar advertises the milestone for seven waves before the milestone content exists. | Set `2.1.0-SNAPSHOT` at F4 and drop the `-SNAPSHOT` in the tag commit after wave 7. |

---

## What the plan gets right (verified, not assumed)

Worth recording so the fixes above do not disturb it:

- **Every RamCore package the plan names exists, in the module it implies** (`loot`, `objective`, `reward`, `cooldown`, `content`, `content.spec`, `store`, `playerdata`, `menu`, `presentation`, `display`, `service`, `metadata`, `metadata.type`, `scheduler`, `region`, `reload`, `encounter`, `party`, `npc`, `trade`, `economy`, `worldinstance`, `schedule`, `selector`, `ai`, `brain`, `pdc`, `exception`, `event`) — the only misattribution is `packet` (M4).
- **Method-level claims all check out:** `InstancedLoot.inMemoryStore()` / `persistentStore(Store, LootPayloadCodec)` (`InstancedLoot.java:30,43`), `LootReward.payload` is `Object` (`LootReward.java:15`), `ObjectiveSubject.party(PartyId)` (`:27`), `EncounterInstance.tick()` / `.damage(UUID, double)` (`:83,96`), `RegionRuleEngine.regionsAt(Position)` (`:52`), `ContentRegistrar.toRuleRegion(ContentId, RegionSpec)` (`:46`), `MenuSession.suspend(InputRequest)` (`:288`), `RewardActionFactories.standard(economy)` registering exactly money/command/message/permission-node-check and **not** `item` (`:40-49`), `WorldBackend.supportsInstances()` (`:16`), `TRADE_RESTOCK_INTERNALS` = `UNSUPPORTED` on Paper (`docs/NMS_COMPATIBILITY.md:88`), `ServiceRegistry` refusing registration after load (`SimpleServiceRegistry.java:42`), `ContentLoader.load(Path)` doing blocking `Files.list` (`:62,65`), `RamPlugin.services()/bind/bindModule/load()` (`:111,99,105,34`).
- **Every RamRPG line citation I checked is exact:** `LootListener.kt:29` (seedless `items.create`), `DamageStages.kt:119-123` (dead `IndicatorStage`), `ItemInstanceServiceImpl.kt:145-152` (`ItemSchemaMigrator` NOOP-only), `AbilityServiceImpl.kt:52-53` and `:78-83` (the `ConcurrentHashMap` cooldowns), `RamRPG.kt:189-199` (`reloadContent` unregistering exactly four registries), `api/items/Items.kt:205-220` (`ItemDefinition`), `SkillsCommand.kt:255,275,283,413` (`RamRPG.get()`), `QuestsGui.kt` = 167 lines, `BuiltinItems.kt` = 197, `BuiltinEnchants.kt` = 106, `BuiltinEntities.kt` = 131, 5,654 LOC main, 14 lang keys all `ramrpg.stat.*`, `QuestServiceImpl.kt` declaring `class QuestService`, `EquipmentListener` enforcing nothing, `DurabilityListener` cancelling globally, `EntityProfileRegistryImpl.mythicResolver` (`:13`), merchant-trade rendering already in `PacketRenderListener.kt:25-56`, anvil rename in `EnchantingListener.kt:80-82`.
- **G6 is correct:** RamCore's `StatOperation` has only `ADD` and `MULTIPLY` (`stat/StatOperation.java:12,15`) against RamRPG's three (`api/stats/Stats.kt:39`). **G1 is correct:** `26.1.2.build.60-stable` and Gradle `9.1.0` match RamCore exactly. **G5 is correct:** `mythicResolver` exists.
- **Schema discipline is sound where it is applied:** exactly one `ItemSchema.CURRENT` bump in Phase 2 (WP-2.1b, wave 8, solo, with a v1 fixture), and the two later "no bump needed" calls (3.3d sockets, 3.4a enchantments) are both correct — `ItemInstanceData.sockets` and `.enchantments: Map<EnchantmentKey,Int>` already exist (`api/items/Items.kt:48,50`).
- **Folia coverage is above average:** F.2 reviewer item 3 catches async Bukkit access, raw schedulers, un-anchored `Promise` continuations and cross-thread entity access; individual WPs flag `DisplaySpawner.spawn`'s Promise (244), `sweepExpired` needing `PlatformScheduler` (172), `PlayerItemConsumeEvent` region affinity (446), spawn-thread affinity (516) and blocking loader I/O (214). M13's two gaps are the exceptions, not the rule.
- **WP counts are self-consistent:** 57 roadmap WPs + 6 Phase-F = 63 (line 583), and the wave table schedules exactly 63. Every roadmap Phase 0-7 task maps to at least one WP except Task 1.6's `StatsGui`/`SkillsGui` bullet (M5).

---

## Verdict

**ACCEPT-WITH-FIXES** — conditional on B1 being resolved before wave 5, and B2-B6 before wave 0b.

The plan is structurally strong: the WP decomposition is right-grained, the RamCore-first discipline is real
(it names the exact class to use in nearly every WP), the schema-bump-per-phase rule is correct, the
solo-wave device for stop-the-world changes is the right instinct, and the risk register anticipates the
genuinely dangerous things (untested migration path, undispatched `TriggeredEffect`, `RamRPG.kt` as a merge
magnet, bulk-content WPs producing plausible-but-wrong data).

It is not ACCEPT because **B1 is fatal and undetected**: the plan's central claim — "content is a file, not a
release" — rests on RamCore API surface that cannot be called from a separate plugin against the shipped jar.
The spike that section B presents as proof (lines 77-84) only ever compiled; it never loaded. Everything from
wave 5 onward is built on that assumption, so a Phase-F runtime spike plus a likely upstream RamCore republish
must land first.

It is not REJECT because every other finding is a local correction — re-cut four waves, add three missing WPs
(CI, permissions, lang backfill) plus one (StatsGui/SkillsGui), fix four Gradle/Store API calls, re-scope
seven tests to their pure layer, and settle the rule-7 waiver. The architecture underneath does not change.

**Minimum set before dispatching wave 0b:** B2, B3, B4 (F2/F3/F5 spec edits), B5, B6, M1, M2, M3 (wave re-cut),
M4 (test classpath), M7 (rule-7 decision), M12 (CI + permissions WPs).
**Minimum set before dispatching wave 5:** B1 resolved and G8 answered.

---

# v2 re-check

Target: `EXECUTION_PLAN.md` v2 (928 lines). Re-verified against the same sources. No project file modified.

## 1. Disposition of every v1 finding

**33 of 33 APPLIED.** Two were applied with a declared variation (the plan owns both at lines 924-928),
and both preserve the substance of the finding. Nothing is PARTIAL; nothing is NOT APPLIED.

| id | status | v2 line(s) | note |
|---|---|---|---|
| B1 | **APPLIED+** | 102 (WP-F0), 132-137 (WP-RC1), 678/709 (W14 hard gate), 844 (G8), 859 (Risk 1) | Citations re-verified exact: `ramcore-paper/build.gradle.kts:69-72` are the four relocates; `ramcore-api/build.gradle.kts:5-12` are the `api(...)` deps. v2 **added** flow-math, which v1 missed — `Position.java:76,82,129` does expose `Vector3d`, verified. |
| B2 | APPLIED | 97 (F2 acceptance names all three test files), 114 (sketch) | |
| B3 | APPLIED | 103 | Forces the choice between the 3-arg `PlayerDataKey.of` and a hand-written `snapshot()`, and says the spec — not the agent — picks. |
| B4 | APPLIED | 103, 105-106 (dataVersion ledger v1→v4) | Uses `Stores.file(..., migrations)` (`Stores.java:84`), not `jsonByUuid` (`:97`). |
| B5 | APPLIED | 679-682 (W15/W16/W17/W18), 238 (1.5d `Dep: 2.1a`), 296 + 681 (2.1a solo) | Verified by full file-list scan: zero same-wave code-file collisions remain (check 3). |
| B6 | APPLIED (variant, declared at 925-927) | 684 (2.1c solo W20), 685 (2.2 W21), 309/316 (shared `InertReason`) | Serialization chosen over merge; the shared `InertReason` type carries the finding's real point. |
| M1 | APPLIED | 207 ("Does not touch `PacketItemRenderer.kt`"), 262 (cooldown lore moved into 1.6b), 671 | |
| M2 | APPLIED | 344-349 (WP-3.0), 687 (solo W23), 391/404/410 (all three say "Does not touch `SkillsCommand.kt`") | |
| M3 | APPLIED | 501/686 (6.2 at W22), 509 (6.1a `Dep: 6.2`), 699 (W35) | |
| M4 | APPLIED | 98 (F3 `testImplementation(ramcore-protocol)`), 260 (module tagged), 263 | The wrong "needed only for `protocol.Protocol`" claim is gone. |
| M5 | APPLIED | 266-270 (WP-1.6c), 675 (W11, after 1.2c releases `SkillsCommand.kt`) | |
| M6 | APPLIED | 415-421 | Old 3.4a and its solo wave deleted; "Rename nothing" with `Enchants.kt:22,23,24,27` cited — re-verified correct. Only `weight`/`skillRequirement` added, as defaulted properties. |
| M7 | APPLIED (variant, declared at 927-928) | 31-37 (section A waiver), 755-756 (rule 7), 799-800 (reviewer item 7) | Waiver chosen over shims, scoped to exactly 1.1a + 1.2a, justified (no published artifact), with an expiry (the JitPack tag). |
| M8 | APPLIED | 481 (`PerkPersistenceMigrationTest`), 482-483 (dataVersion v4, "empty never null") | Wording now matches 4.1b. |
| M9 | APPLIED | 193 | Restated as `ContentDeserializeException` per `RewardActionFactory.java:29`. |
| M10 | APPLIED | 168, 200, 234, 302, 362, 461, 488, 514 (pure layer named per test), 139-143 (WP-RC2), 760-761 (rule 10), 795-797 (reviewer item 5) | `CommandTestHarness` / `MenuClickContexts` confirmed absent from RamCore (zero matches). |
| M11 | APPLIED | 437 (4.1a `Dep: 1.5b, 1.2b`), 501 (6.2 `Dep: 1.5b, 2.3a`), 509 (6.1a `Dep: 6.2, 1.5b`) | 5.1a keeps `Dep: 4.1a` with a stated real reason (the `perk_point` reward un-stub) — accepted. |
| M12 | APPLIED | 101 (F7 CI), 244-248 (1.5e permissions), 595-600 (7.5 README/CONTENT + `ContentDocCoverageTest`), 812 (checklist item 3) | |
| M13 | APPLIED | 272-278 (1.7a), 280-287 (1.7b), 286 (acceptance), 817-818 (scope gate) | **Verified mechanically:** the only WPs listing `RamRPG.kt` are F5 (W6 solo), 1.7a (W12 solo), 1.7b (W13 solo). No post-1.7b WP does. |
| m1 | APPLIED | 225-226 | "all nine triggers" in both places; `custom:<content-id>` added. |
| m2 | APPLIED | 224 ("the only WP that adds it"), 460 ("Does not modify `api/effects/Effects.kt`") | |
| m3 | APPLIED | 224-226 | Three registries + `EffectConditionRegistryUnknownIdTest`. |
| m4 | APPLIED | 78-79, 276, 283 | Verified: 23 `lateinit` at `RamRPG.kt:89-112` and `:116`; the 18/4/1 breakdown is exactly right. |
| m5 | APPLIED | 96 | `git rm --cached` + `.gitignore` in one commit. |
| m6 | APPLIED | 873-883 | Tags restated by wave; W18/W22/W28/W34/W40/W42 are all real wave boundaries. |
| m7 | APPLIED | 123-124 | PS column deleted; the wave table declared the single source. |
| m8 | APPLIED | 757-758 (roadmap rule 8 restored verbatim), 759 (new rule 9), 38-41 (deviation owned in section A) | |
| m9 | APPLIED | 822-825 (checklist item 11 = DoD gate, Paper **and** Folia), 100 (SMOKE_TEST append-only), 737 (template field) | |
| m10 | APPLIED | 154-159 (WP-0.2 solo W8), 819-820 (checklist item 9 replaces the grep) | |
| m11 | APPLIED | 347 | Folded into WP-3.0. |
| m12 | APPLIED | 254 | `Promise<DisplayHandle<TextDisplay>>`. |
| m13 | APPLIED | 443 (4.1a sweep), 587 (7.3 cron), 745-748 (rule 4), 790-791 (reviewer item 3) | |
| m14 | APPLIED | 99, 682, 851 | `2.1.0-SNAPSHOT`, dropped at the W18 tag. |

## 2. Structural check 1 — wave coverage and graph/Dep agreement

**Wave coverage: PASS.** 72 WPs, each in exactly one wave. Counted by hand across W0-W42: 71 scheduled
plus RC2 on the floating "any" row = 72, matching the header's "8 Phase-F + 2 cross-repo + 62 roadmap"
(I counted 8 / 2 / 62 independently in the catalog; `1.4` superseded and `5.2` deferred correctly
excluded). 43 waves, W0-W42. No WP scheduled twice; none missing.

**Dependency ordering: PASS.** I walked all 65 Dep declarations against the wave table. Every
dependency merges in a strictly earlier wave. Tightest pairs: 1.5d `Dep: 2.1a` (W17→W18), 3.0
`Dep: 5.3, 1.5e` (W22/W18→W23), 1.7a `Dep: …1.2b, 1.6c` (W11→W12), 6.1a `Dep: 6.2` (W22→W35),
5.3 `Dep: 2.1c` (W20→W22), 3.4b `Dep: 3.1b` (W25→W27). **No inversion anywhere.**

**Graph/Dep agreement: 8 mismatches — all benign, but line 606's claim is overstated.**

| mermaid edge | the WP's Dep says | effect |
|---|---|---|
| `W01 --> W02` (618) | 0.2 `Dep: F5` | graph stricter; harmless |
| `W02 --> W11a / W12a / W16a` (619) | all three say `Dep: 0.1` | graph stricter; harmless |
| `W12c --> W16c` (620) | 1.6c `Dep: 0.1` | graph encodes the real `SkillsCommand.kt` scheduling constraint the Dep field omits |
| no `W15d --> W23b` | 2.3b `Dep: 2.3a, 1.5d` | graph weaker; W18 < W21, safe |
| no `W15d --> W32a` | 3.2a `Dep: 3.1a, 1.5d` | graph weaker; W18 < W25, safe |
| no `W11a --> W32c` | 3.2c `Dep: 3.2a, 1.1a` | graph weaker; W9 < W26, safe |
| no `W31b --> W42b` | 4.2b `Dep: 4.2a, 3.1b` | graph weaker; W25 < W31, safe |

None can cause an out-of-order dispatch: the wave table is consistent with the **union** of graph and
Dep lists. Fix: regenerate the mermaid from the Dep lists, and soften line 606.

## 3. Structural check 2 — same-wave file collisions

**Code files: PASS — zero collisions.** I diffed the file lists of every multi-WP wave (W3, W7, W9,
W11, W16, W18, W21, W22, W25, W26, W28, W30, W34, W38, W40, W41). No two concurrent WPs share a single
`.kt`, `.conf`, `.yml` or `.gradle.kts` path. This is a genuine, verified fix — B5, B6, M1, M2 and M3
are repaired in the schedule, not merely claimed. The widest wave (W25, six WPs) is clean precisely
because WP-3.0 ran at W23. `api/items/Items.kt` is touched only by solo WPs (2.1a W17, 2.1b W19,
2.1c W20) and by 5.3 at W22, its sole Items.kt editor that wave. `RamRPG.kt` is touched only by three
solo WPs.

**`docs/DESIGN.md` and `lang/en_us.json`: FAIL the tolerance test.** The merge checklist does not handle
this conflict class. 34 WPs declare `lang yes` and ~30 declare a DESIGN section; eleven waves run two or
more concurrently, peaking at W25 with **five concurrent `lang/en_us.json` edits and five concurrent
`docs/DESIGN.md` appends**. F.3 offers only item 5 ("conflicts resolved on main") and item 9
(`LangKeyCoverageTest`). That test detects a *dropped* key after the fact (the Kotlin reference survives
the clobber), so lang is partially covered. **`docs/DESIGN.md` is covered by nothing at all** — a section
silently lost in a merge is undetectable, and the "numbers are written down" discipline that every
downstream WP cites rests on that file. See **N4**.

## 4. New findings introduced or first-identified in v2

| id | sev | v2 line | finding | concrete fix |
|---|---|---|---|---|
| **N1** | MAJOR | 616 (`RC1 --> F5`), 669-670, 102 | **The whole plan is now serialized behind a cross-repo republish and a live-server spike.** The edge `RC1 --> F5` plus wave order W4 (F0) → W5 (RC1) → W6 (F5) makes eleven RamRPG WPs (0.1, 0.2, 1.1a, 1.2a, 1.2c, 1.3, 1.6a, 1.6b, 1.6c, 1.7a, 1.7b) wait on a fix only WP-1.5a needs — F5 uses `store`/`playerdata` and touches no configurate type. Worse, F0's own acceptance concedes it may be unrunnable ("or document exact steps if no server is available"), and G8 is gated on F0's evidence, so a missing test server stalls everything from W4. v1's fix was "hard-gate wave 5 (1.5a)", not "gate the entire plan". | Delete the `RC1 --> F5` edge; run F5 at W4 and W5-W13 as scheduled, dispatching F0 + RC1 as a **parallel background cross-repo track**. Keep the existing W14 hard gate — the only place the fix is actually needed. Also make G8 answerable from the static evidence alone (`build.gradle.kts:69-72` vs `:5-12` is dispositive), so F0 is a confirmation step, not a blocker. |
| **N2** | MAJOR | 671 (W7), 723 (template) | **W7 runs WP-0.1, which *creates* `docs/DESIGN.md`, concurrently with WP-1.3, which must *append* to it** (DESIGN section "Ability cooldown scopes and persistence", line 209). In 1.3's worktree the file does not exist. The implementer template's mandatory READ FIRST item 2 is `docs/DESIGN.md` — "numbers and formulas already decided" — which no W7 agent can read. Carried over from v1; my v1 review missed it. | Split W7: W7a = 0.1 solo (it is the prerequisite the template assumes), W7b = {1.3, 1.6b}. Costs one wave, removes the bootstrap paradox for every downstream WP. |
| **N3** | MAJOR | 191 (1.2b), 284 (1.7b module list), 675 (W11) | **WP-1.2b creates `core/modules/RewardModule.kt` at W11, but the `bindModule` seam does not exist until WP-1.7b at W13, and 1.2b's file list excludes `RamRPG.kt`** — so nothing registers the reward factories for two waves. It compiles, passes its unit tests (which test the factories, not the wiring) and passes review, while being dead code. 1.7b's file list also claims to create `RewardModule.kt`, so the two collide on ownership. 1.2b cannot simply move after 1.7b: 1.7a's `Dep` (line 272) includes 1.2b. | 1.2b registers in `RamRPG.kt` like every other pre-1.7b WP — permitted, since no other W11 WP touches it — and 1.7b converts that registration into `RewardModule`. Drop `core/modules/RewardModule.kt` from 1.2b's file list; keep it in 1.7b's. |
| **N4** | MAJOR | 808-825 (F.3), 689 (W25) | **No merge protocol for `docs/DESIGN.md` or `lang/en_us.json`** across eleven multi-WP waves (check 3). DESIGN.md has zero detection; a clobbered section is invisible and every downstream WP cites that file. | Add F.3 item 5a: *"For `docs/DESIGN.md` and `lang/en_us.json`, merge by union — take both sides' additions, never either side wholesale."* Add `.gitattributes` with `docs/DESIGN.md merge=union`. Structurally better: each WP writes `docs/design/<wp-id>-<slug>.md` with `DESIGN.md` as a generated index, making the conflict impossible; plus `DesignSectionCoverageTest` (every WP id in the tracker has a section file) as the DESIGN.md equivalent of `LangKeyCoverageTest`. |
| **N5** | MINOR | 606 | "the graph and the table were regenerated together and agree" — 8 mismatches (check 2). None harmful, but the claim invites an orchestrator to trust the graph over the table. | Regenerate the mermaid from the Dep lists; restate as "the table is authoritative; the graph is a rendering of it". |
| **N6** | MINOR | 322 (2.3a), 284 (1.7b), 337 (5.3) | **Module seam list incomplete, one assignment semantically wrong.** 2.3a registers *damage pipeline stages* in `core/modules/UiModule.kt`; 1.7b's 14-module list has no combat/stat module. 5.3 creates `core/modules/SetModule.kt`, which 1.7b does not seed. Conversely `LootModule`, `QuestModule` and `EconomyModule` are seeded but never referenced by any later WP. | Add `CombatModule` and `SetModule` to 1.7b's list; point 2.3a at `CombatModule`; reference or drop the three unused seams. |
| **N7** | MINOR | 134 | RC1's problem statement lists snakeyaml among the four packages `ramcore-api` "declares as `api(...)`". It does not — snakeyaml arrives transitively via `configurate-yaml` (`ramcore-api/build.gradle.kts:7`). The relocation and the ABI conclusion still stand. | Reword to "three declared `api(...)` deps plus snakeyaml transitively via `configurate-yaml`". |

**No new BLOCKER.** I probed the riskiest new content specifically: WP-RC1's proposed fix is sound
(un-relocated configurate in RamCore's plugin jar is visible to RamRPG through the existing
`join-classpath: true`, and bstats/kotlinx correctly stay relocated); the 72/43 arithmetic is
self-consistent; and every new source citation checks out exactly against source
(`build.gradle.kts:69-72`, `:5-12`, `Position.java:76,82,129`, `random/VariableAmount`,
`Enchants.kt:22-27`, `RamRPG.kt:89-112`/`:116`, `RamRPG.kt:267-302`, `Items.kt:189-199`,
`AbilityServiceImpl.kt:70,75,81`, and the absence of `CommandTestHarness`).

## v2 verdict

**ACCEPT-WITH-FIXES.**

v2 applies all 33 findings, and the two hardest ones structurally rather than cosmetically: the wave
schedule was genuinely re-cut from the file lists (zero same-wave code collisions across 16 multi-WP
waves, verified by hand) and the `RamRPG.kt` merge-magnet problem is closed by the 1.7a/1.7b split with
a scope gate that makes violations mechanically detectable. The B1 analysis was independently deepened
(flow-math). Nothing was rejected; both declared variations are defensible.

The four remaining MAJORs are pre-dispatch spec edits, not re-architecture: delete one mermaid edge and
re-order W4-W6 (N1), split W7 (N2), move one file from 1.2b's list to 1.7b's (N3), add one merge rule
and one `.gitattributes` line (N4). None requires re-planning a phase. Apply them and the plan is
dispatchable.

**Before dispatching W0:** N4 (the merge protocol applies from W7 onward and is cheapest to add now).
**Before dispatching W4:** N1 (take F0/RC1 off the critical path).
**Before dispatching W7:** N2. **Before dispatching W11:** N3.
